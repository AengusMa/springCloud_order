spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: remote
    stream:
      binders:
        outputMessage:
          group: order
  output:
    ansi:
      enabled: always
  rabbitmq:
    host: 111.231.228.108
  redis:
    host: 111.231.228.108
    port: 6379
eureka:
  client:
    service-url:
      defaultZone: http://111.231.228.108:8761/eureka/
