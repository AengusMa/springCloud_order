spring:
  application:
    name: product
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: win7
  output:
    ansi:
      enabled: always
eureka:
  client:
    service-url:
      defaultZone: http://192.168.99.100:8761/eureka/