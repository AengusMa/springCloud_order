spring:
  application:
    name: user
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: remote
eureka:
  client:
    service-url:
      defaultZone: http://111.231.228.108:8761/eureka/